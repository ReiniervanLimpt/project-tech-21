<header>
    <nav>
        <ul>
            <li> <a href="/">home</a> </li>
            {{#if user}}
            {{else}}
            <li> <a href="/register">register</a> </li>
            {{/if}}
        </ul>
    </nav>
    {{#if user}}
      <a href="/logout">Log out</a>
    {{else}}
      <button class="login-button">Log in</button>
    {{/if}}
</header>
